{
  "name": "@ocrbase/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate",
    "db:seed-public": "bun run src/seed-public.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@ocrbase/env": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "nanoid": "catalog:",
    "pg": "^8.18.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@ocrbase/config": "workspace:*",
    "@types/pg": "^8.16.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "catalog:"
  }
}
